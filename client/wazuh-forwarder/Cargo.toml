[package]
name = "wazuh_forwarder"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "palo_alto_forwarder"
path = "src/palo_alto_main.rs"

[[bin]]
name = "tshark_forwarder"
path = "src/tshark_main.rs"


[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4.0", features = ["derive"] } # Added for command line parsing
crossbeam-channel = "0.5" # Keep for inter-thread communication, as it's very efficient
csv = "1.2" # Added for Palo Alto CSV parsing
fern = { version = "0.7", features = ["colored"] }
lazy_static = "1.4"
log = "0.4"
regex = "1.5"
reqwest = { version = "0.12", features = ["json"] } # REMOVED "blocking" feature
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
signal-hook = "0.3"
sha2 = "0.10"
thiserror = "2.0"
tokio = { version = "1", features = ["full"] } # Added for async operations
toml = "0.8" # Added for configuration file parsing
tracing = "0.1"
parking_lot = "0.12" # High-performance mutex for memory pool
dashmap = "5.5" # Concurrent hashmap for circuit breakers
once_cell = "1.19" # For static initialization of memory pool
lru = "0.16.0"
maxminddb = "0.24"

[dev-dependencies]
proptest = "1.6"
